<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:gravity="center"
    android:orientation="vertical">


    <!--    Button d' entrainement -->
    <TextView
        android:id="@+id/Title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/title"
        android:layout_marginBottom="70dp"
        android:textColor="@color/colorTitleMenu"
        android:textSize="30sp" />

    <Button
        android:textSize="30sp"
        android:id="@+id/home_new_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/btnTraining"
        />
<!--        Button pour commencer le jeu -->
    <Button
        android:textSize="30sp"
        android:id="@+id/home_continue_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/btnBegin"
        />
<!--        Button pour l'option-->
    <Button
        android:textSize="30sp"
        android:id="@+id/home_high_score_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/btnOption"
        />

</LinearLayout>
